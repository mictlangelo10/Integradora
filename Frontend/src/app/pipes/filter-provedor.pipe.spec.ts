import { FilterProvedorPipe } from './filter-provedor.pipe';

describe('FilterProvedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProvedorPipe();
    expect(pipe).toBeTruthy();
  });
});
