import { FilterClasePipe } from './filter-clase.pipe';

describe('FilterClasePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClasePipe();
    expect(pipe).toBeTruthy();
  });
});
